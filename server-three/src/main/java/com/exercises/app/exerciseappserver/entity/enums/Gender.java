package com.exercises.app.exerciseappserver.entity.enums;

/* usign enum in JPA will force us to cast & pass this type of object everything we want to use that attribute.
Hence, we will skip it, as it is making code less readable.
 */
public enum Gender {
    M,
    F
}
